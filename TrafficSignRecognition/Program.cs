
using AutoVisionProjekt;
using OpenCvSharp;
using OpenCvSharp.Extensions;

namespace ZnackyUI
{
   internal static class Program
   {
      [STAThread]
      static void Main()
      {
         Application.Run(new Form1());
      }
   }
}